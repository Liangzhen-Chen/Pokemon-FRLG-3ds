# 3DS 程序说明

当前程序是双屏图形与输入演示，尚不包含可玩的火红／叶绿游戏流程。上屏中央显示 240×160 图形，下屏显示输入状态。

在安装 devkitPro `3ds-dev` 工具链后，从仓库根目录运行 `make build-3ds`。生成文件为 `platform/3ds/frlg-3ds-p2c3.3dsx` 和对应的 `.smdh`。

方向键滚动上屏背景，A 键切换图层优先级。触控和按键状态显示在下屏，同时按 X + Y 退出。演示不读取或写入游戏存档。
